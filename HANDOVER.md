# 작업 인수인계 문서

**작성 시각**: 2025-11-18
**현재 상태**: 안건별 검색 시스템 구현 중 - ChromaDB 재구축 필요

---

## 🎯 현재 작업 목표

**검색 방식 변경**: 청크 단위 벡터 검색 → 안건 단위로 그룹핑하여 표시

### 아키텍처
```
[검색 흐름]
사용자 쿼리
  ↓
ChromaDB 청크 검색 (벡터 유사도)
  ↓
청크별 agenda_id 추출
  ↓
안건별 그룹핑 (최고 유사도 선택)
  ↓
SQLite에서 안건 상세 정보 조회
  ↓
프론트엔드에 안건 단위로 표시
```

---

## 📋 최근 작업 내용

### ✅ 완료된 작업

1. **검색 파이프라인 최적화**
   - Query Analyzer: "답변생성불가" 개념 제거
   - Metadata Validator: 유사 발언자 자동 보정 (예: "주용태" → "경제실장 주용태")
   - Search Executor: 원본 쿼리 보존 (정보 손실 방지)
   - 코사인 유사도 계산 수정: `(2 - distance) / 2`

2. **프론트엔드 UI**
   - search.html: 검색 결과 표시 ("검색결과: N건")
   - 모바일 IP 표시 기능 추가 (backend_server.py)

3. **데이터 구조화**
   - `insert_to_chromadb.py` 수정: 각 청크에 `agenda_id` 메타데이터 추가
   - `create_agenda_database.py` 생성: SQLite DB 구조 설계

4. **문서화**
   - PROPOSAL.md: 해커톤 기획서 (실제 구현 내용 반영)
   - 프로젝트 파일 정리: old/, markdown/ 폴더 분리

### 🔄 진행 중인 작업

**안건별 검색 시스템 구현**

1. ✅ ChromaDB 청크에 `agenda_id` 추가 (`insert_to_chromadb.py` 수정 완료)
2. ✅ SQLite DB 스크립트 작성 (`create_agenda_database.py` 완료)
3. ⏳ ChromaDB 재구축 (agenda_id 메타데이터 추가 필요)
4. ⏳ SQLite DB 생성
5. ⏳ backend_server.py 검색 로직 수정
6. ⏳ 상세 페이지 구현 (details.html)

---

## 🚨 현재 블로커

**문제**: ChromaDB 폴더 삭제 불가
- **원인**: Python 프로세스 또는 VSCode가 ChromaDB 파일 잠금
- **경로**: `C:\Users\SBA\Project\seoulloc\chroma_db`
- **필요 작업**: VSCode 완전 종료 후 폴더 삭제

**왜 삭제해야 하나?**
- 기존 청크에는 `agenda_id` 메타데이터가 없음
- 새로운 메타데이터 구조로 재구축 필요

---

## 🔧 VSCode 재시작 후 작업 순서

### 1단계: ChromaDB 삭제 및 재구축

#### 1-1. ChromaDB 폴더 삭제
```bash
# 방법 A: Windows 탐색기에서 직접 삭제
C:\Users\SBA\Project\seoulloc\chroma_db 폴더 삭제

# 방법 B: PowerShell (관리자)
cd C:\Users\SBA\Project\seoulloc
Remove-Item -Recurse -Force chroma_db
```

#### 1-2. ChromaDB 재구축 (agenda_id 포함)
```bash
# Seoul 환경 활성화
conda activate seoul

# ChromaDB 재생성 (agenda_id 메타데이터 추가됨)
python insert_to_chromadb.py
```

**예상 결과**:
```
📋 발견된 안건: 5개
   - meeting_20251117_195534_agenda_000: 기타발언
   - meeting_20251117_195534_agenda_001: 서울특별시 인공지능산업 육성 및 지원 조례안...
   - meeting_20251117_195534_agenda_002: 서울특별시 인공지능윤리 기반 조성에 관한 조례안...
   ...

✅ 삽입 완료!
📊 최종 문서 수: 169개
```

**확인 사항**:
- 각 청크에 `agenda_id` 메타데이터가 포함되어야 함
- 예: `"agenda_id": "meeting_20251117_195534_agenda_001"`

---

### 2단계: SQLite DB 생성

```bash
python create_agenda_database.py
```

**작업 내용**:
- `agendas.db` 파일 생성
- 2개 테이블 생성:
  - `agendas`: 안건 상세 정보 (제목, 발언자, 전체 텍스트 등)
  - `agenda_chunks`: 안건-청크 매핑

**예상 결과**:
```
================================================================================
안건별 SQLite 데이터베이스 생성
================================================================================

✅ 데이터베이스 테이블 생성 완료

📁 발견된 JSON 파일: 2개

📄 처리 중: meeting_20251117_195534.json
   안건 수: 5개
   ✓ [1] 기타발언 (발언자: 1명, 청크: 2개)
   ✓ [2] 서울특별시 인공지능산업 육성 및 지원 조례안... (발언자: 2명, 청크: 4개)
   ...

================================================================================
✅ 완료! 총 10개 안건이 저장되었습니다.
================================================================================

📊 데이터베이스 통계:
   안건 수: 10개
   청크 수: 169개
   저장 파일: agendas.db
```

---

### 3단계: backend_server.py 검색 로직 수정

**현재 구조**:
```python
# 현재: 청크 검색 → 결과 직접 반환
chunk_results = searcher.search(query)
return chunk_results  # 청크 단위
```

**변경 필요**:
```python
# 변경: 청크 검색 → 안건 그룹핑 → SQLite 조회
# 1. ChromaDB 청크 검색
chunk_results = chroma_collection.query(query, n_results=20)

# 2. 안건별 그룹핑 (agenda_id 기준)
agenda_scores = {}  # {agenda_id: max_similarity}
for chunk in chunk_results:
    agenda_id = chunk['metadata']['agenda_id']
    similarity = (2 - chunk['distance']) / 2

    if agenda_id not in agenda_scores:
        agenda_scores[agenda_id] = similarity
    else:
        agenda_scores[agenda_id] = max(agenda_scores[agenda_id], similarity)

# 3. 유사도 순 정렬
sorted_agendas = sorted(agenda_scores.items(), key=lambda x: x[1], reverse=True)[:10]

# 4. SQLite에서 안건 상세 조회
import sqlite3
conn = sqlite3.connect('agendas.db')
cursor = conn.cursor()

agenda_ids = [a[0] for a in sorted_agendas]
cursor.execute(f'''
    SELECT * FROM agendas WHERE agenda_id IN ({','.join(['?']*len(agenda_ids))})
''', agenda_ids)

agendas = cursor.fetchall()

# 5. 응답 생성
results = []
for agenda in agendas:
    results.append({
        "agenda_id": agenda[0],
        "title": agenda[1],
        "speaker": agenda[5],
        "similarity": agenda_scores[agenda[0]],
        ...
    })

return results  # 안건 단위
```

**이 작업은 아직 안했음! 다음에 할 것**

---

### 4단계: 상세 페이지 구현 (예정)

**파일**: `html/details.html` (이미 존재)

**API 추가 필요**:
```python
# backend_server.py
@app.get("/api/agenda/{agenda_id}")
async def get_agenda_detail(agenda_id: str):
    conn = sqlite3.connect('agendas.db')
    cursor = conn.cursor()

    cursor.execute('SELECT * FROM agendas WHERE agenda_id = ?', (agenda_id,))
    agenda = cursor.fetchone()

    return {
        "agenda_id": agenda[0],
        "title": agenda[1],
        "meeting_title": agenda[2],
        "full_text": agenda[10],  # combined_text
        ...
    }
```

**프론트엔드 연결**:
- search.html에서 안건 카드 클릭 시
- `/details?agenda_id=xxx`로 이동
- details.html에서 API 호출하여 상세 정보 표시

---

## 📁 주요 파일 설명

### 새로 생성된 파일

1. **`create_agenda_database.py`** ⭐ 신규
   - SQLite DB 생성 및 안건 데이터 삽입
   - 테이블: `agendas`, `agenda_chunks`
   - JSON → SQLite 변환

2. **`PROPOSAL.md`** ⭐ 신규
   - 해커톤 기획서 (실제 구현 내용 기반)
   - 시스템 아키텍처, 기술 스택, 개발 현황

### 수정된 파일

1. **`insert_to_chromadb.py`** ⭐ 수정됨
   - 81-95번 줄: 안건별 인덱싱 추가
   - 각 청크에 `agenda_id` 메타데이터 자동 부여
   - 예: `"agenda_id": "meeting_001_agenda_002"`

2. **`backend_server.py`**
   - 로컬 IP 표시 기능 추가 (267-299번 줄)
   - 모바일 접근용 IP 출력

3. **`query_analyzer.py`**
   - "답변생성불가" 개념 제거 (67-72번 줄)
   - 애매한 질문도 원본 쿼리로 검색

4. **`metadata_validator.py`**
   - "답변생성불가" 검증 제거 (68-73번 줄)
   - 유사 발언자 자동 보정 추가 (111-120번 줄)

5. **`search.html`**
   - "총 N개의 결과가 검색되었습니다" → "검색결과: N건" (95, 182번 줄)

---

## 🗂️ 데이터베이스 구조

### ChromaDB: `seoul_council_meetings` (청크 컬렉션)

**메타데이터 구조** (수정됨):
```python
{
    "meeting_title": "제332회 AI경쟁력강화특별위원회",
    "meeting_date": "2025.09.10",
    "meeting_url": "https://...",
    "speaker": "이용균 위원",
    "agenda": "AI 조례안",
    "agenda_id": "meeting_001_agenda_002",  # ⭐ 신규 추가
    "chunk_index": 5,
    "source_file": "meeting_001"
}
```

### SQLite: `agendas.db` (안건 DB)

**테이블 1: agendas**
```sql
agenda_id TEXT PRIMARY KEY              -- "meeting_001_agenda_002"
agenda_title TEXT NOT NULL              -- "AI 조례안"
meeting_title TEXT                      -- "제332회 AI경쟁력강화특별위원회"
meeting_date TEXT                       -- "2025.09.10"
meeting_url TEXT
main_speaker TEXT                       -- "이용균 위원" (가장 많이 발언)
all_speakers TEXT                       -- "이용균, 김OO, 박OO"
speaker_count INTEGER
chunk_count INTEGER
chunk_ids TEXT                          -- "chunk_005,chunk_006,chunk_007"
combined_text TEXT                      -- 전체 텍스트 (길이 제한 없음!)
status TEXT                             -- "원안가결", "계류" 등
created_at TIMESTAMP
```

**테이블 2: agenda_chunks** (매핑 테이블)
```sql
chunk_id TEXT PRIMARY KEY
agenda_id TEXT
chunk_index INTEGER
speaker TEXT
text_preview TEXT
```

---

## 🔍 검색 흐름 비교

### 이전 방식 (청크 단위)
```
사용자: "AI 경쟁력"
  ↓
ChromaDB 검색
  ↓
결과:
[chunk_005 (AI 조례, 유사도 0.87),
 chunk_006 (AI 조례, 유사도 0.85),  ← 중복!
 chunk_012 (청년 조례, 유사도 0.82)]
  ↓
프론트엔드: 3개 카드 표시 (중복 포함)
```

### 새로운 방식 (안건 단위)
```
사용자: "AI 경쟁력"
  ↓
ChromaDB 청크 검색
  ↓
중간 결과:
[chunk_005 (agenda_002, 유사도 0.87),
 chunk_006 (agenda_002, 유사도 0.85),
 chunk_012 (agenda_003, 유사도 0.82)]
  ↓
안건별 그룹핑:
{agenda_002: 0.87,  ← 최고 유사도만
 agenda_003: 0.82}
  ↓
SQLite 조회:
[{title: "AI 조례", similarity: 0.87},
 {title: "청년 조례", similarity: 0.82}]
  ↓
프론트엔드: 2개 카드 표시 (안건 단위, 중복 제거)
```

---

## 🐛 알려진 이슈

### 이슈 1: ChromaDB 폴더 잠금
- **현상**: chroma_db 폴더 삭제 실패
- **해결**: VSCode 완전 종료 후 삭제

### 이슈 2: insert_agendas_to_chromadb.py 토큰 초과
- **현상**: 안건 텍스트가 너무 길어서 OpenAI 임베딩 실패 (22,301 토큰)
- **해결**: 30,000자로 자르기 (이미 수정됨)
- **참고**: 이 파일은 사용 안함! SQLite 방식으로 변경

---

## 📊 현재 데이터 상태

### result_txt 폴더
- **파일 수**: 2개 JSON
- **총 청크**: 169개
- **안건 수**: 약 10개 (추정)

### ChromaDB (재구축 필요)
- **현재**: 169개 청크 (agenda_id 없음)
- **재구축 후**: 169개 청크 (agenda_id 포함)

### SQLite (생성 예정)
- **파일**: `agendas.db`
- **안건 수**: 약 10개
- **청크 매핑**: 169개

---

## ✅ VSCode 재시작 후 체크리스트

```
[ ] 1. ChromaDB 폴더 삭제
      - 경로: C:\Users\SBA\Project\seoulloc\chroma_db
      - 확인: 폴더가 완전히 삭제됨

[ ] 2. ChromaDB 재구축
      - 명령: python insert_to_chromadb.py
      - 확인: "발견된 안건: 5개" 메시지 출력
      - 확인: agenda_id 메타데이터 포함됨

[ ] 3. SQLite DB 생성
      - 명령: python create_agenda_database.py
      - 확인: agendas.db 파일 생성됨
      - 확인: 10개 안건 삽입 완료

[ ] 4. backend_server.py 수정
      - 청크 검색 → 안건 그룹핑 로직 추가
      - SQLite 조회 코드 추가

[ ] 5. 검색 테스트
      - 서버 실행: python backend_server.py
      - 검색: "AI 경쟁력"
      - 확인: 안건 단위로 결과 표시

[ ] 6. 상세 페이지 구현
      - API: /api/agenda/{agenda_id}
      - 프론트엔드: details.html 연결
```

---

## 🚀 다음 주요 작업

1. **backend_server.py 검색 로직 수정** ⭐ 우선순위 1
   - 청크 검색 → 안건 그룹핑
   - SQLite 조회 통합

2. **상세 페이지 구현**
   - details.html API 연결
   - 핵심 의제, 발언자별 입장 정리

3. **RAG 챗봇 (예정)**
   - LangChain/Langgraph
   - 청크 컬렉션 활용

---

## 💡 중요 참고사항

- **검색 정확도**: 청크 단위 벡터 검색 유지 (세밀한 매칭)
- **UI 표시**: 안건 단위로 그룹핑 (깔끔한 UX)
- **정보 보존**: SQLite에 전체 텍스트 저장 (길이 제한 없음)
- **확장성**: 챗봇은 청크 컬렉션 사용 (세밀한 컨텍스트)

**핵심**: ChromaDB(벡터 검색) + SQLite(메타데이터) 하이브리드 방식

---

**마지막 업데이트**: 2025-11-18
**다음 작업자**: VSCode 재시작 후 체크리스트 순서대로 진행하세요!
